import { BuildColourPipe } from './build-colour.pipe';

describe('BuildColourPipe', () => {
  it('create an instance', () => {
    const pipe = new BuildColourPipe();
    expect(pipe).toBeTruthy();
  });
});
